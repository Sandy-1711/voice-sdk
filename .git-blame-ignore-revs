# Commits that only reformat. Ignored so `git blame` shows the last real
# author of a line rather than the person who ran Prettier.
#
# Enable locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Add one full 40-character SHA per line, newest last.
